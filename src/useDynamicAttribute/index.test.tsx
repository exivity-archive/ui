import useDynamicAttribute from '.'

test('length of enriched array should be the same as the passed in array', () => {
  // interface ITest { id: number }
  // const test = [{ id: 1 },{ id: 2 }, { id: 3 }]

  // const enriched = useDynamicAttribute<ITest,
})
