import React from 'react'
import { InputWithIcon, InputWithIconProps } from '../InputWithIcon/InputWithIcon'

export const TextInputWithIcon = (props: InputWithIconProps) => (
  <InputWithIcon type='text' {...props} />
)
