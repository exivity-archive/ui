import React from 'react'
import { storiesOf } from '@storybook/react'

storiesOf('docs|Getting started 👋', module)
  .add('Installation', () => <div />)
  .add('ThemeProvider', () => <div />)
  .add('Custom fonts', () => <div />)
  .add('Components', () => <div />)
